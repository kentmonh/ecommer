<% @title = "Products" %>

<h2 class="display-4 text-center">All Products</h2>

<div>
  <%= form_with(url: filter_products_path, method: :get, local: true) do %>
    <div class="form-row align-items-center">
      <div class="col-auto">
        <label class="form-check-label">
          Filter Products
        </label>
      </div>
      <div class="col-auto">
        <%= select_tag 'filter_product', raw("<option>All Products</option><option>Sale Products</option><option>New Products</option>"), :onchange => "this.form.submit();", class: 'form-control' %>
      </div>
    </div>
  <% end %>
</div>

<% if flash[:add_cart] %>
  <div class="alert alert-success" role="alert">
    <%= flash[:add_cart] %>
  </div>
<% end %>

<div class="card-deck">
  <%= render @products %>
</div>

<%= paginate @products %>